DATABASE_URL="postgresql://tracker:tracker123@localhost:5432/training_tracker"
JWT_SECRET="generate-a-random-64-char-secret-here"
# Optional: Required for update checks on private GitHub repositories
# GITHUB_TOKEN="ghp_your-personal-access-token"
# Update channel: "stable" (default, production releases only) or "dev" (includes pre-releases)
# UPDATE_CHANNEL="stable"
